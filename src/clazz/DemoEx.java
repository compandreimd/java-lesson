package clazz;

public class DemoEx {
    //creati a class care are 2  valori (prin (runtime) scanner)
    public static void main(String[] args) {
        DemoThis t = new DemoThis("A", 100);
        System.out.println(t.getNume());
        System.out.println(t.getSalariu());
    }
}
